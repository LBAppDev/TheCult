## Packages
framer-motion | Essential for smooth animations and transitions in a game interface
lucide-react | Already in stack but crucial for icons
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["'Cinzel', serif"],
  body: ["'Inter', sans-serif"],
  mono: ["'Fira Code', monospace"],
}

The app requires polling for game state updates every 1 second.
Theme is strictly dark mode with a mysterious/cult aesthetic.
